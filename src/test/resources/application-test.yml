# SPDX-FileCopyrightText: 2025 Digg - Agency for Digital Government
#
# SPDX-License-Identifier: EUPL-1.2

spring:
  config:
    import: optional:classpath:application-main.yml

# Application Properties
properties:
  apisecret: my_secret_key
  public-paths:
    - "/actuator/**"
    - "/v3/api-docs/**"
    - "/swagger-ui.html"
    - "/swagger-ui/**"
    - "/public/**"
    - "/wua"
    - "/accounts"
    - "/v0/accounts"
  walletprovider:
    baseurl: http://localhost:${wiremock.provider.port:0}
    wua-path: /wallet-provider/wallet-unit-attestation
  attributeattestation:
    baseurl: http://localhost:${wiremock.attestation.port:0}
    paths:
      post: /attestation
      get-by-id: /attestation
      get-by-key: /attestation/user
  walletaccount:
    baseurl: http://localhost:${wiremock.account.port:0}
    paths:
      post: /account
      get: /account
  wallet-r2ps:
    baseurl: http://localhost:${wiremock.r2ps.port:0}
    paths:
      post: /service
      new-state: /new_state
  challenge-cache:
    ttl-seconds: 10

